import os
# select the directory whose content you want to the list
directory_path = '/ New folder'
# use the os module to lis the directory content
content = os.listdir(directory_path)
#print the content of the directory
print(content)
      